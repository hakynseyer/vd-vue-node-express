export interface InterAlert {
  color?: string;
  mensaje?: string;
  activar: boolean;
  timer?: number;
}

export interface URL_Pventa {
  icono: string;
  descripcion: string;
  ruta: string;
}

export interface Menu_Default {
  icono: string;
  titulo: string;
}
